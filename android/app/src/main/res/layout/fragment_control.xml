<?xml version="1.0" encoding="utf-8"?>
<!--
  fragment_control.xml
  Landscape layout: two joysticks side-by-side, status bar in the middle.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Left joystick — translate (fwd/back + strafe) -->
    <com.makis.tankcontrol.JoystickView
        android:id="@+id/joystickLeft"
        android:layout_width="220dp"
        android:layout_height="220dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="24dp"/>

    <!-- Right joystick — rotate -->
    <com.makis.tankcontrol.JoystickView
        android:id="@+id/joystickRight"
        android:layout_width="220dp"
        android:layout_height="220dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="24dp"/>

    <!-- Centre column -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintStart_toEndOf="@id/joystickLeft"
        app:layout_constraintEnd_toStartOf="@id/joystickRight"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/textStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="vx=0.00 vy=0.00 ω=0.00"
            android:textColor="#CCCCCC"
            android:textSize="14sp"
            android:layout_marginBottom="16dp"/>

        <ToggleButton
            android:id="@+id/buttonStop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textOn="RESUME"
            android:textOff="STOP"
            android:backgroundTint="@color/accent"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
